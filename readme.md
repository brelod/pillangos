# PillangOS

PillangOS is an experimental Project of building custom Linux distributions. It's based on the LFS project and
the package maneger has some similarities to the one in Alpine Linux.

To build the project use the bash and docker scripts in the specified stages one after another.

